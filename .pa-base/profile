# .pa-base/profile — the scrml-site PA boot manifest. /boot REHYDRATES from this; it does not re-derive.
# Assembled by /flobase 2026-07-22 (first assembly). Re-run `/flobase reinit` when stack/scope changes.
# The assembled context region lives in .claude/CLAUDE.md (flobase-fenced). This file POINTS at the
# normative contract; it does not replace it. Authority: pa.md (repo root) + hand-off.md.

IDENTITY:
  git config user.name -> slugify -> first token -> ../scrml-support/pa-profile-<slug>.md
  bryan maclee -> bryan -> pa-profile-bryan.md   (default: bryan, if unset/unmatched)

STACK:  scrml (primary) — 106 .scrml: app.scrml (site shell) · pages/ (99: the WIKI) ·
        components/ (5) · lib/provenance.scrml. Compiler = LINKED DEPENDENCY, bun-run. NEVER
        vendored — the linked dep dogfoods the real adopter install path.
        Secondary: thin plain-ESM Node (scripts/build-artifacts.mjs). NO TypeScript, no tsconfig.

  ⚑ THE COMPILER IS ../scrml — NOT ../scrmlTS (operator-corrected 2026-07-22).
    ../scrmlTS = STALE/RETIRED. HEAD 9e607bad (S172, 2026-06-07). Its PA is not running.
    ../scrml   = LIVE. HEAD df6d269c (S279, 2026-07-22), v0.7.1, ~107 sessions ahead. Same examples/
                 corpus (both flagships), same compiler/src/api.js entry point.
    REWIRE LANDED f2367b2 + c5f3a14 (2026-07-22): package.json `scrml: link:scrml`,
    node_modules/scrml -> ../../scrml; build-artifacts.mjs + serve.sh resolve through it; data/
    regenerated against v0.7.1 and BYTE-IDENTICAL on re-run; source made v0.7.1-conformant
    (`scrml build` exit 0). scrml package.json has NO `exports` field -> deep subpaths
    (scrml/compiler/src/api.js, scrml/examples/*.scrml) resolve; bin is {"scrml": ...}.
    KEEP the dep pointed at ../scrml. Do NOT re-link scrmlts.
STAGE:  mid-flight — working app, active dev. inc1 LANDED (compile-transparent viewer, real .js.map
        bidirectional hover-provenance). inc2 #1 LANDED (column-precision highlights) + #2 LANDED
        (25-triage-board as 2nd flagship + data-driven selector). Live inc2 backlog, no rot.
SCOPE:  MEDIUM (was small; changed 2026-07-22 by the wiki migration) — 106 source files / ~100
        routes. data/ + dist/ are GENERATED, not source. Single operator.
        One god-file: pages/showcase.scrml @ ~730 LOC (the dissector; every showcase fork lands there).
        RECONSIDER the `maps` module — it was dropped on "small scope", which no longer holds.
GATE:   runtime-verify (PRIMARY, merge-blocking) + build (SECONDARY, exit-0 only). Ratified 2026-07-22.
  CROSS-MACHINE (per-machine, gitignored — do FIRST on a fresh clone/machine):
    cd ../scrml && bun link   then   cd ../scrml-site && bun install
    verify: node_modules/scrml/compiler/bin/scrml.js AND
            node_modules/scrml/node_modules/playwright/index.js both resolve.
    Requires the sibling layout <root>/scrml + <root>/scrml-site. Playwright is NOT a devDependency
    here — it is borrowed from the compiler repo through the link. All scripts resolve THROUGH that
    link (machine-independent); env overrides PLAYWRIGHT / SCRML exist if the layout differs.
    NEVER re-hardcode an absolute /home/<user>/ path — that broke all three scripts until 2026-07-22.
  - PRIMARY, TWO SCRIPTS — both must exit 0. `bash scripts/serve.sh` (port 8787), then:
      `node scripts/wiki-verify.mjs`  — SITE-WIDE (5 assertions): every emitted route resolves 200
        (route list DERIVED from dist/, so a new page is covered the moment it compiles) · shell +
        outlet render · SOFT NAV genuinely soft (window stamp survives an in-site click) ·
        code-block contrast (fg/bg luminance separated) · no uncaught page errors.
      `node scripts/gold-verify.mjs` — the /showcase provenance gate (11 assertions). GATE_BASE
        overrides the origin; it targets /showcase, NOT / (which is now the wiki landing).
    Real-browser Chromium
    verification via Playwright. Import playwright by ABSOLUTE path from ../scrml/node_modules
    (present + verified 2026-07-22; ../scrmlTS also has a copy but prefer the LIVE repo). A bare
    specifier will NOT resolve from /tmp. CommonJS default-import form.
    Assertions: flagship iframe mounts+runs · forward hover (source line -> exact sub-line JS cells) ·
    reverse hover (JS cell -> source line + siblings) · unmapped cell clears · selector re-renders
    source + engine graph + iframe + JS. This IS the S146 serve-before-push discipline.
    Precedent: S1 gold-verify PASS; S2 15/15 PASS. User MAY waive on strong verification (S151) —
    the user's call, never the PA's.
  - SECONDARY: `bun run build:artifacts` must exit 0. BYTE-IDENTITY IS DELIBERATELY NOT BLOCKING —
    data/ tracks a MOVING sibling compiler, so drift is expected, not a defect.
    POST-REWIRE 2026-07-22: exit 0 AND byte-identical on re-run — reproducibility against v0.7.1 is
    RESTORED. **CORRECTED 2026-07-27: a data/ diff does NOT reliably mean "the compiler moved."**
    The dep is a SYMLINK to a live working tree, so data/ reflects whichever BRANCH the sibling has
    checked out. Our committed artifacts were once baked from `feat/wave1c-nav`, never merged to
    main, and nothing recorded it. Nothing pins build-artifacts.mjs; CI pins, the precompute does not.
    The .js.map DRIVES hover-provenance: any compiler bump REQUIRES a full re-run of the PRIMARY
    gate, never a rubber stamp.
  - TYPES gate (added 2026-07-22): `node_modules/.bin/scrml build . --output <tmp>` must EXIT 0.
    NOT redundant with serve — `scrml dev` emits LENIENTLY (it emitted despite 9 type errors, so the
    site ran while a production build was broken). Only `scrml build` is the real shape-check.
    Language invariants that bit us at the v0.7.1 rewire, now conformant — do NOT reintroduce:
      `any` IS NOT A TYPE (E-TYPE-ANY-FORBIDDEN). Declare a named struct and return `[Struct]`.
        Inline object return types mis-compile (pre-existing friction), so ALWAYS a named struct.
      `not` IS THE ABSENCE VALUE, not boolean negation (E-TYPE-045). Use `!expr` to negate;
        `expr is not` to test absence.
  - `ERR_INCOMPLETE_CHUNKED_ENCODING` in the console = benign dev-server static-chunking noise.
  - No test suite exists. Do not fake one as a gate; the artifact is the truth here.
  - CODE-BLOCK CONTRAST is gated for a reason: the typography layer ships
    `.prose-slate :where(code) { color: #0f172a }` — a LIGHT-theme value — which rendered every
    fenced example slate-900-on-slate-900 (INVISIBLE) across the reference pages, the wiki's core
    content. Fixed in app.scrml with `pre code { color: inherit !important }`. Do not remove.
  - LINT SURFACE (triaged 2026-07-22; `scrml build` is GREEN with all of it present):
      W-DEAD-FUNCTION x2 (buildJsCellLines, enginesOf) = COMPILER FALSE POSITIVE — the analysis does
        not traverse arrow-callback bodies. Both ARE called (index.scrml:385, :398). A same-named
        export elsewhere masks it (that is why toLines escapes). DO NOT "clean up" these functions.
      W-TAILWIND-UNRECOGNIZED-CLASS x17 (dev only) = COMPILER FALSE POSITIVE — every flagged class is
        defined in our own #{} style blocks. Ignore.
      E-ROUTE-001 x7 = inert. Route-placement heuristic firing on plain array/dict indexing in pure
        client helpers; this app wires 0 server routes.
      W-STYLE-CONFLICT-POSSIBLE x21 = advisory. Fires on the .tok-* classes, which are mutually
        exclusive by construction (lineClass() returns exactly one) but not provably so. Accepted.
      Both false-positive classes reported to ../scrml 2026-07-22.

DEPLOY (added 2026-07-28; the profile predates it):
  scrml.dev is SERVED BY THIS REPO — GitHub Actions -> Pages, `.github/workflows/deploy.yml`.
  Apex-only by construction: nav links are ABSOLUTE and the compiler has NO base-path flag, so a
    project-Pages path prefix breaks navigation. Do not try to host it under a subpath.
  BOTH the compiler ref AND the toolchain are PINNED (SCRML_REF + bun-version). An unpinned `latest`
    broke the deploy on 2026-07-28. The compiler arrives from git with NO node_modules — CI installs
    explicitly (TWICE: acorn/astring live only in the stray nested compiler/package.json). Never rely
    on bun auto-install.
  BUMPING SCRML_REF REQUIRES A FULL LOCAL GATE RUN FIRST. As of 2026-07-28 the pin is HELD at
    50478f0e (S287) because gold-verify is 9/11 against S291 — advancing it ships that regression.
  Route/directory collisions 404 on static hosts: a page emitting `X.html` beside a directory `X/`
    with no index is fine under `scrml dev` and dead on Pages. GATE THE ARTIFACT, NOT THE DEV SERVER.

MODULES:
  loaded:    CORE (~/.claude/CLAUDE.md) · stack-pack-scrml · role-pa · continuity · cross-pa-notify
  on-demand: role-dpa + deliberation (runtime, on a design fork only) · role-spa (if the inc2 backlog
             becomes heavy enough to queue)
  dropped:   stack-pack-ts (no TS — one plain .mjs) · maps (small scope; RECONSIDER if index.scrml
             keeps growing) · vcs-drive (single main, no satellite branches) · role-vpa (no
             maintenance surface) · role-cpa (single project; master PA owns cross-project) ·
             dock (0 coverage)

PROFILE-A read-set (FULL boot — the DEFAULT):
  1. .claude/CLAUDE.md            (auto-loaded — the flobase-assembled region)
  2. pa.md                        IN FULL   (the normative project contract)
  3. hand-off.md                  IN FULL   (session state; newest session at top)
  4. handOffs/incoming/*.md       (inbox; move processed -> read/)
  5. ../scrml-support/pa-profile-bryan.md   (personal layer — register + voice ledger pointer)
PROFILE-B read-set (THIN — ONE landed-spec arc whose brief carries the sweep):
  .claude/CLAUDE.md + hand-off.md + the named spec sections from the brief.
  scope_blindness guardrail: escalate to A if the arc needs design/context the thin reads don't carry.
  DEFAULT = Profile A when the user gives no signal. The USER picks; the PA may recommend, never auto-switch.

CONTINUITY store:
  live hand-off: hand-off.md (repo root)   changelog: none yet (folded into hand-off)
  inbox: handOffs/incoming/ -> read/       outbox: sibling handOffs/incoming/ (the ONE write exception)
  memory: ~/.claude/projects/-home-bryan-scrmlMaster-scrml-site/memory/
  NO delta-log (small scope, few sessions — the hand-off carries state directly). Wrap via /wrap.

CROSS-PA (dropbox protocol; hook = .flobase/hooks/notify-inbox.sh via .claude/settings.json UserPromptSubmit):
  filename YYYY-MM-DD-HHMM-<from>-to-<target>-<slug>.md ; frontmatter from/to/date/subject/needs/status.
  **COMMIT-ON-ARRIVAL (operator-ruled 2026-07-28) — THE INBOX IS PER-CLONE.** `handOffs/incoming/` is
    git-TRACKED, but a dropped file is UNTRACKED until someone commits it, so a message written to one
    clone is invisible to every other. Four of ours sat unread for two days on exactly this. **Writing
    the file is not delivering it — COMMIT it.** Their main is protected (required check `gate`, 0
    approvals, enforce_admins) so delivery goes via branch+PR. If their checkout is mid-session (ours
    was, on `wrap/s291-xps` with an unpushed commit), use a DETACHED WORKTREE — never switch their HEAD
    — then remove the worktree and delete the now-redundant untracked copies (verify byte-identical
    first; they block the sibling's next rebase).
  needs: reply|action -> surfaced at the TURN boundary. needs: fyi -> boot-time inbox read only.
  LIVE CHANNELS:
    scrml  (../scrml/handOffs/incoming/) — THE compiler PA. Codegen output-shape changes land a notice
      HERE and trigger a provenance-pane rebuild. Compiler bugs found here go OUT to its inbox.
    master (../handOffs/incoming/) — push coordination.
  DEAD CHANNEL — scrmlTS. Do NOT route anything there; its PA is not running (last commit 2026-06-07).
    Historical traffic: our scandir bug report + its reply (handOffs/incoming/read/). The scandir bug
    is SINCE FIXED in ../scrml (api.js:134 scanDirectory — dot-entry + SCAN_SKIP_DIRS skip + lstatSync;
    the fix comment credits "reported by scrml-site S154"). We never saw the landing notice because we
    were watching the WRONG INBOX — the absence-of-evidence trap. Sessions 1-3 waited on a dead channel.

PROJECT-INTENT (RESET by the operator 2026-07-22): **scrml-site IS THE WIKI OF SCRML** — the
  official documentation site, scrml.dev, built in scrml. NOT a novelty dashboard. The
  compile-transparent viewer is ONE PAGE of it (/showcase), not the product: the page that PROVES
  the language is real, sitting beside the reference that explains it.
  The 98-page wiki was MIGRATED IN from scrml/docs/website/ on 2026-07-22 (it had been written but
  never built or deployed, sitting in the compiler repo since 2026-06-19). Structure: index ·
  getting-started · learn/ · about/ · articles/ (17) · reference/{elements,keywords,errors,contexts}
  · showcase · dashboard(stub).
  The ORIGINAL showcase framing, still true of /showcase:
  Live compiled flagship (60% left) beside stacked right-40% panes — .scrml source TOP / engine
  "what-comes-next" transition diagram MIDDLE / tabbed compiled output BOTTOM (JS·HTML·CSS) — with
  bidirectional hover-provenance driven by the real compiler-emitted .js.map. Extracted from
  scrmlTS/docs/website-viewer/ on 2026-06-02 (S154) so scrmlTS stays pure language/compiler.

CONVENTIONS (load-bearing; full text in .claude/CLAUDE.md + pa.md):
  THE MAPPING IS ALWAYS REAL (real .js.map bytes · real VLQ decode · real engine-graph JSON · real live
    flagship). Only syntax-highlight COLORING is faked (S148-permitted). Never fake a mapping.
  Precompute is SINGLE-FILE ON PURPOSE — the parked engine-graph multi-file write-loop bug only
    mis-writes when many inputs compile together. Do NOT batch build-artifacts.mjs.
  Test assets STAY IN THE COMPILER REPO — flagships referenced cross-repo from the linked package's
    examples/; this repo owns only the precomputed data/ outputs. (Post-rewire: scrml/examples/.)
  Adding a flagship = 3 edits: FLAGSHIPS (build-artifacts.mjs) + flagshipList() (index.scrml) + one
    if=-gated literal-src iframe line.
  Iframe src is DELIBERATELY NOT reactive (two if=-gated literal-src iframes). Reactive-attribute
    interpolation on src is documented friction — do not "simplify."
  COMPILER-FRICTION WORKAROUNDS — all RE-VERIFIED against ../scrml v0.7.1 on 2026-07-22:
    [LOAD-BEARING, KEEP] selectFlagship() clears every list cell to [] before refilling.
      Tier-0 `${ for ... lift }` NESTED lists do NOT reconcile when the backing cell is replaced in
      place — the nested subtree keeps rendering the PREVIOUS value. FLAT lists were fixed by
      df6d269c (source pane 177->152 updates correctly WITHOUT the workaround), but the engine pane
      (for engines > for states > for next) still renders mario's Big,Cape,Fire,Small after
      switching to triage, and jsCellLines>cells leaves 7 stale cells (192 vs 185). SILENT
      wrong-render — the pane looks plausible and is lying. Routing through [] forces a full
      recreate. REPORTED to ../scrml 2026-07-22 (nested-list-reconcile-stale). Drop it only when
      that lands, and re-verify with assertion 11 of the gate.
    [FIXED UPSTREAM, RETIRED] serve.sh explicit-file-list -> now `scrml dev .`. scanDirectory
      (api.js:134) skips dot-entries + SCAN_SKIP_DIRS and uses lstatSync.
    [PARTIAL — CORRECTED 2026-07-22] dev-server watcher: PAGE edits DO hot-recompile (probed, an
      edit reached dist/ + the served page within 12s). SHELL edits (app.scrml) DO NOT recompose the
      already-emitted page HTML — `scrml dev` keeps serving the old page markup while the lint
      correctly updates. An app.scrml change therefore looks like a no-op. `rm -rf dist` + restart
      serve.sh after ANY app.scrml edit. Reported to ../scrml 2026-07-22 (lint-false-positives-and-
      shell-watcher). The earlier blanket "watcher is fixed" verdict was based on a page-only probe.
  Commits: conventional-ish prefixes (`inc2:` `chore:` `docs(pa):` `wrap(sN):`).
  REMOTE: origin = git@github.com:bryanmaclee/scrml-site.git — SSH, NEVER HTTPS (HTTPS routes through
    git-credential-manager, which hangs in headless shells).
  Commit to main ONLY after explicit user authorization in the CURRENT session. Never --no-verify
    without explicit authorization.
  bryan: field-direct register · no preambles · no reflexive hedging · REAL DISAGREEMENT IS REQUESTED ·
    the `---` answer delimiter · operator-authoritative context budget · Profile A/B picked by the user.
