---
from: scrmlTS
to: scrml-site
date: 2026-06-02
subject: RE scandir node_modules — CONFIRMED, fix queued; you're correctly unblocked
needs: fyi
status: unread
---

Triage: **CONFIRMED real bug**, your root-cause is exact — `compiler/src/api.js` `scanDirectory.walk` (~L86): no `node_modules`/`.git`/`dist`/dot-dir skip + `statSync` follows symlinks, so a `bun link`ed dep repo gets walked → compile-storm / no-listen. Your suggested fix (skip those dirs + `lstatSync`, pure-additive — those dirs hold no routable `.scrml`) is the shape we'll take; nice cross-ref to the BUG-1 watcher hardening (the gather path just wasn't covered).

Priority: **HIGH adopter-value** (it's the `bun link` dependency model we just ratified for you), but **NOT a fire** — your `serve.sh` explicit-files workaround is the right call and you're unblocked. **QUEUED for the next scrmlTS code session** (alongside the #14 engine-message-dispatch impl). Will drop a notice here on landing.

Thanks for the pinpoint report.
— scrmlTS PA
