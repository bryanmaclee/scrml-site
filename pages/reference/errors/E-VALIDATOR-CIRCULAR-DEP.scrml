// scrml.dev /reference/errors/E-VALIDATOR-CIRCULAR-DEP
//
// Reference page for E-VALIDATOR-CIRCULAR-DEP. Validator predicate arguments form a cycle.
//
// SPEC anchors: §31.4, §53.6.1, §55.11, §55.15, §34.
//
// Written 2026-07-26. Prose derived from the compiler's own emission message in
// compiler/src; the reproducer below is the exact source compiled against the
// linked compiler to verify what this code does.

<page>

    // REFERENCE SIDEBAR opt-in — static, page-scoped, zero JS. The nav is
    // authored once in app.scrml and hidden by default; this shows it and
    // marks THIS page's link active. See scripts/gen-reference-nav.mjs.
    #{
        /* !important is load-order, not laziness: the shell's app.css is linked
           AFTER this page's stylesheet, so its `.refnav{display:none}` default
           wins at equal specificity. */
        .ref-shell { display: grid !important; }
        .refnav { display: block !important; }
        .refnav-link[data-ref="errors/E-VALIDATOR-CIRCULAR-DEP"] {
            color: rgb(125 211 252);
            background-color: rgb(14 165 233 / 0.10);
            border-left-color: rgb(56 189 248);
            font-weight: 600;
        }
    }

    <article class="prose prose-slate max-w-3xl">

        <header class="not-prose mb-8 pb-6 border-b border-slate-200">
            <div class="text-xs font-mono text-slate-500 mb-2">
                Reference &rsaquo; Errors
            </div>
            <h1 class="text-4xl font-bold tracking-tight text-rose-700 font-mono">
                E-VALIDATOR-CIRCULAR-DEP
            </h1>
            <p class="text-lg text-slate-600 mt-2">
                Validator predicate arguments form a cycle.
            </p>
            <div class="flex flex-wrap gap-3 mt-4 text-xs">
                <span class="px-2 py-1 bg-rose-50 text-rose-700 rounded font-semibold">
                    Error
                </span>
                <span class="px-2 py-1 bg-slate-100 text-slate-700 rounded">
                    Compile-time
                </span>
                <a href="https://github.com/bryanmaclee/scrml/blob/main/compiler/SPEC.md#31"
                   class="text-blue-700 hover:text-blue-900 underline">
                    SPEC §31.4 (normative)
                </a>
            </div>
        </header>

        <h2 class="text-2xl font-semibold text-slate-900 mt-10 mb-4">
            What it means
        </h2>

        <p class="text-slate-700 leading-relaxed">
            Cross-field validators may reference other fields — <code>eq()</code>, comparisons against sibling cells — and those references form a dependency graph. That graph must be a <strong>DAG</strong>: validation is evaluated in dependency order, so a cycle has no valid evaluation order. The code fires both for a direct self-reference (a field whose validator reads itself) and for a longer cycle across several fields.
        </p>

        <h2 class="text-2xl font-semibold text-slate-900 mt-10 mb-4">
            Minimal reproducer
        </h2>

        <p class="text-slate-700 leading-relaxed mb-4">
            This is the exact source compiled against the linked compiler and verified to produce <code>E-VALIDATOR-CIRCULAR-DEP</code>.
        </p>

        <pre class="bg-slate-900 text-slate-100 p-4 rounded-md text-sm overflow-x-auto"><code>&lt;program title="p"&gt;
    &lt;signup&gt;
        &lt;email req eq(@signup.email)&gt; = &lt;input type="email"/&gt;
    &lt;/&gt;
    &lt;errors of=@signup.email/&gt;
&lt;/&gt;</code></pre>

        <h2 class="text-2xl font-semibold text-slate-900 mt-10 mb-4">
            How to fix
        </h2>

        <ol class="space-y-3 text-slate-700 list-decimal list-outside ml-6">
            <li>
                <strong>Break the self-reference.</strong>
                A field's own validator should constrain its value, not compare it to itself; the self-reference is usually a typo for a sibling field.
            </li>
            <li>
                <strong>Make the dependency one-directional.</strong>
                For a confirm-password pair, only the confirmation field references the original — not both.
            </li>
        </ol>

        <h2 class="text-2xl font-semibold text-slate-900 mt-10 mb-4">
            Related
        </h2>

        <ul class="space-y-1 text-slate-700">
            <li>
                <a href="/reference/keywords/req" class="text-blue-700 hover:text-blue-900 underline">req</a>
            </li>
            <li>
                <a href="/reference/elements/errors" class="text-blue-700 hover:text-blue-900 underline">&lt;errors&gt;</a>
            </li>
        </ul>

        <h2 class="text-2xl font-semibold text-slate-900 mt-10 mb-4">
            Specification
        </h2>

        <p class="text-slate-700 leading-relaxed">
            Normative text: <strong>§31.4, §53.6.1, §55.11, §55.15, §34</strong>. Spec lives at
            <a href="https://github.com/bryanmaclee/scrml/blob/main/compiler/SPEC.md"
               class="text-blue-700 hover:text-blue-900 underline">
                compiler/SPEC.md
            </a>.
        </p>

        <p class="mt-10 text-sm">
            <a href="/reference" class="text-blue-700 hover:text-blue-900 underline">
                &larr; Reference
            </a>
        </p>

    </article>

</>
