// scrml.dev /reference/errors/E-CHANNEL-SHARED-MODIFIER
//
// Reference page for E-CHANNEL-SHARED-MODIFIER. The `@shared` modifier is removed from the language.
//
// SPEC anchors: §38.4, §38.4.1, §34.
//
// Written 2026-07-26. Prose derived from the compiler's own emission message in
// compiler/src; the reproducer below is the exact source compiled against the
// linked compiler to verify what this code does.

<page>

    // REFERENCE SIDEBAR opt-in — static, page-scoped, zero JS. The nav is
    // authored once in app.scrml and hidden by default; this shows it and
    // marks THIS page's link active. See scripts/gen-reference-nav.mjs.
    #{
        /* !important is load-order, not laziness: the shell's app.css is linked
           AFTER this page's stylesheet, so its `.refnav{display:none}` default
           wins at equal specificity. */
        .ref-shell { display: grid !important; }
        .refnav { display: block !important; }
        .refnav-link[data-ref="errors/E-CHANNEL-SHARED-MODIFIER"] {
            color: rgb(125 211 252);
            background-color: rgb(14 165 233 / 0.10);
            border-left-color: rgb(56 189 248);
            font-weight: 600;
        }
    }

    <article class="prose prose-slate max-w-3xl">

        <header class="not-prose mb-8 pb-6 border-b border-slate-200">
            <div class="text-xs font-mono text-slate-500 mb-2">
                Reference &rsaquo; Errors
            </div>
            <h1 class="text-4xl font-bold tracking-tight text-rose-700 font-mono">
                E-CHANNEL-SHARED-MODIFIER
            </h1>
            <p class="text-lg text-slate-600 mt-2">
                The <code>@shared</code> modifier is removed from the language.
            </p>
            <div class="flex flex-wrap gap-3 mt-4 text-xs">
                <span class="px-2 py-1 bg-rose-50 text-rose-700 rounded font-semibold">
                    Error
                </span>
                <span class="px-2 py-1 bg-slate-100 text-slate-700 rounded">
                    Compile-time
                </span>
                <a href="https://github.com/bryanmaclee/scrml/blob/main/compiler/SPEC.md#38"
                   class="text-blue-700 hover:text-blue-900 underline">
                    SPEC §38.4 (normative)
                </a>
            </div>
        </header>

        <h2 class="text-2xl font-semibold text-slate-900 mt-10 mb-4">
            What it means
        </h2>

        <p class="text-slate-700 leading-relaxed">
            <code>@shared</code> was the old marker for a realtime-synced cell. It is gone. Sync is now <strong>structural</strong>: a reactive cell declared inside a <code>&lt;channel&gt;</code> body auto-syncs by virtue of where it is declared, and no marker is required. Because the modifier no longer means anything, keeping it would imply a guarantee the compiler is not making — so it is rejected rather than ignored.
        </p>

        <h2 class="text-2xl font-semibold text-slate-900 mt-10 mb-4">
            Minimal reproducer
        </h2>

        <p class="text-slate-700 leading-relaxed mb-4">
            This is the exact source compiled against the linked compiler and verified to produce <code>E-CHANNEL-SHARED-MODIFIER</code>.
        </p>

        <pre class="bg-slate-900 text-slate-100 p-4 rounded-md text-sm overflow-x-auto"><code>$&#123;
    @shared messages = []
&#125;
&lt;program title="p"&gt;
    &lt;p&gt;chat&lt;/p&gt;
&lt;/&gt;</code></pre>

        <h2 class="text-2xl font-semibold text-slate-900 mt-10 mb-4">
            How to fix
        </h2>

        <ol class="space-y-3 text-slate-700 list-decimal list-outside ml-6">
            <li>
                <strong>Delete the keyword and declare inside the channel body.</strong>
                <code>&lt;messages&gt; = []</code> inside <code>&lt;channel&gt;</code> is the canonical synced form.
            </li>
            <li>
                <strong>Use a plain cell if it should <em>not</em> sync.</strong>
                A cell declared outside any channel body stays local. Placement is now the whole signal.
            </li>
        </ol>

        <h2 class="text-2xl font-semibold text-slate-900 mt-10 mb-4">
            Related
        </h2>

        <ul class="space-y-1 text-slate-700">
            <li>
                <a href="/reference/elements/channel" class="text-blue-700 hover:text-blue-900 underline">&lt;channel&gt;</a>
            </li>
            <li>
                <a href="/reference/errors/E-CHANNEL-OUTSIDE-PROGRAM" class="text-rose-700 hover:text-rose-900 underline">E-CHANNEL-OUTSIDE-PROGRAM</a>
            </li>
        </ul>

        <h2 class="text-2xl font-semibold text-slate-900 mt-10 mb-4">
            Specification
        </h2>

        <p class="text-slate-700 leading-relaxed">
            Normative text: <strong>§38.4, §38.4.1, §34</strong>. Spec lives at
            <a href="https://github.com/bryanmaclee/scrml/blob/main/compiler/SPEC.md"
               class="text-blue-700 hover:text-blue-900 underline">
                compiler/SPEC.md
            </a>.
        </p>

        <p class="mt-10 text-sm">
            <a href="/reference" class="text-blue-700 hover:text-blue-900 underline">
                &larr; Reference
            </a>
        </p>

    </article>

</>
